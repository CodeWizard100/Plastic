#pragma once

class CameraClass {
    /*
    public:
    
        CameraClass();
        ~CameraClass();
        int Initalize(ShaderClass* shaderclass);
        void SetCameraPosition(float x, float y, float z);
        void SetCameraRotation(float angle, float x, float y, float z);
        void SetCameraScale(float scale);
    private:
        ShaderClass* shader;
    */
};